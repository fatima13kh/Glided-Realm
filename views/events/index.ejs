<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Events</title>
</head>
<body>
    <h1>All Events</h1>

<ul>
  <% events.forEach((event) => { %>
  <li>
    An event titled "<%= event.title %>" of type "<%= event.type %>".
  </li>
  <% }) %>
</ul>
    <% if(user) { %>
    <a href="/events/new">Add a New Event</a>
    <% } %>
</body>
</html>